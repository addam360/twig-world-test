/* Imports */
var arrayGrouper = require('./array-grouper');

/* Split Array into Even Groups */
var inputArray = [2, 4, 6, 1.4, 2.3, 'a'];
var arrayDivisor = 2;
console.log(arrayGrouper.groupArrayElements(inputArray, arrayDivisor));
